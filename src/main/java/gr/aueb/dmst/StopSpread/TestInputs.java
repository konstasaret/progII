package gr.aueb.dmst.StopSpread;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.fail;

import java.io.IOException;
import java.util.Scanner;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class TestInputs {
	Inputs obj;

/*	@Before
	public void setUp() {
		System.out.println("Starting testing of Inputs.java");
		obj = new Inputs();
	}
	
	@After
	public void finish() {
		System.out.println("Finished testing of Inputs.java");
	}
	
	
	@Test
	public void isNumWithinBounds() {
		//periptoseis errors
		assertTrue(obj.isNumberAllowed(1, 10, 5)); // Mesa sta bounds
		assertFalse(obj.isNumberAllowed(1, 10, 0)); // Eksw apo aristera
		assertFalse(obj.isNumberAllowed(1, 10, 11)); // Eksw apo deksia
//		x: input
		
		//lower < x < upper
		//lower < upper < x tf
		//x < lower < upper ft
		//upper < x < lower ff
	}

 */

}
